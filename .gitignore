.DS_Store

# Agent
/agent/node_modules/
/agent/dist/

# Extension (if you add build tooling later)
/chrome-extension/dist/

# Docker volume data (config + secrets)
/data/

# Local env (secrets/keys) — never commit. Use .env.example as the template.
.env
*.env
!.env.example
